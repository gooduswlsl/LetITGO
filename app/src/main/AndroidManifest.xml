<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sook.cs.letitgo">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_INTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_SMS" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".shared.PermissionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".shared.ProfileActivity" />
        <activity android:name=".shared.ProfileIconActivity" />
        <activity android:name=".shared.IndexActivity" />
        <activity android:name=".customer.MainActivity" />
        <activity
            android:name=".customer.customer_main"
            android:windowSoftInputMode="stateAlwaysHidden" />
        <activity android:name=".customer.customer_dialog_store" />
        <activity
            android:name=".customer.customer_dialog_menu"
            android:theme="@android:style/Theme.Holo.Light.Dialog" />
        "
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->

        <activity android:name=".seller.Login" />
        <activity
            android:name=".seller.Seller_main"
            android:theme="@style/Toolbar" />
        <activity
            android:name=".shared.Login1"
            android:label="회원가입" />
        <activity
            android:name=".shared.Login2"
            android:label="이메일로 회원가입" />
        <activity
            android:name=".shared.Login2_1_store_info"
            android:label="매장 정보" />
        <activity android:name=".shared.GroupActivity" />

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.sook.cs.letitgo.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <activity
            android:name=".shared.Login_2_1_1_searchAddress"
            android:label="주소 찾기"
            android:theme="@android:style/Theme.Dialog" />
        <activity android:name=".shared.Login_finish" />
        <activity android:name=".seller.MenuPicAddActivity" />
        <activity android:name=".seller.MenuPicEditActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".customer.customer_store_detail"></activity>

    </application>

</manifest>